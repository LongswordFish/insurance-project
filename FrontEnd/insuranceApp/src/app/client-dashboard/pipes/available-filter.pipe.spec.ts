import { AvailableFilterPipe } from './available-filter.pipe';

describe('AvailableFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AvailableFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
