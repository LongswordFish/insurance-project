import { AvailableProductsFilterPipe } from './available-products-filter.pipe';

describe('AvailableProductsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AvailableProductsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
